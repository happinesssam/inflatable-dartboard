namespace utterlySuperb.pixiApp.app.model{
    export interface AppSettingsOptions{
        renderWidth:number;
        renderHeight:number;
        appWidth?:number;
        appHeight?:number;
        antialias?:boolean;
        containerId?:string;
    }
}